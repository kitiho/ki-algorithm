寻找子串考虑使用双指针的滑动窗口。
左右指针从开始出发。
右指针向右扩大窗口，直到满足T中的字符都有了，此时为一个可行解。
然后左指针向右滑动，缩小窗口，目的为寻找最优解。
此时有两种情况，
如果不要了的字符不是T中的字符，则继续向右滑动，此时仍然满足条件，
如果不要了的字符是T中的字符，则维护的字典和needType对应处理，看是否仍然满足条件。
当不满足条件了，则重复，右指针滑动扩大窗口，直到右指针滑不动了。
此处优化点为，维护两个数据。
一个用来记录还需要的字符的数量，一个用来记录是否所有字符都有了。
